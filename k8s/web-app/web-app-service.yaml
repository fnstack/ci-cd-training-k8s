apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  namespace: training-cicd
  labels:
    app: web-app
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: web-app